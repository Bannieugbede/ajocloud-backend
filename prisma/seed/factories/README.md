# Seed factories

Reusable deterministic factories belong here as seed coverage grows.

Nothing has needed one yet: each seeder's fixtures are specific to its feature.
Extract a factory when the same shape is built in two or more seeders.
